---
# Ingress: Terrain CDN only
# NOTE: /api/elevation goes through api-gateway, NOT this ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eu-elevation-ingress
  namespace: nekazari
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
  labels:
    app: eu-elevation
    module: eu-elevation
spec:
  rules:
    - host: terrain.robotika.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: terrain-cdn-service
                port:
                  number: 80
